import 'package:flutter/material.dart';

class OpcoesPage extends StatelessWidget {
  const OpcoesPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Opções')));
  }
}
